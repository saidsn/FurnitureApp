import React from "react";

const Background = ({style}) => {
  return (
    <div className="background" style={style}>
      {/* use background opacity for searcresult and contact components; */}
    </div>
  );
};

export default Background;
